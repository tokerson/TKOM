print((-1*3)*(2+(1+2)));

head([1]);

tail([1]);
tail([1,2]);

if( 2 == 1) {
    def Int x = 2;

} elsif( 1 ){
} else {
}

//those are supposed to throw an Exception while Runtime
//head();
//head([]);
//tail();
//tail([]);