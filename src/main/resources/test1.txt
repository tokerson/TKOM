print((-1*3)*(2+(1+2)));

head([1]);

tail([1]);
tail([1,2]);

//x(1);
x(x(1));
x([1]);

if( 2 > 1 && ( 1>2 || 1 && 0)) {
    def Int x = 2;
} elsif( 1 ){
} else {
}

if( -1 =< -11 ){

}

def Int x(Int:x){
    return 0;
}

//those are supposed to throw an Exception while Runtime
//head();
//head([]);
//tail();
//tail([]);